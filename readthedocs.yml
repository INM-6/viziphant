# readthedocs version
version: 2

build:
    image: latest

sphinx:
  builder: html
  configuration: doc/conf.py

python:
    install:
        - requirements: requirements/requirements-docs.txt
        - method: pip
          path: .
